<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sh.ppy.osulazer"
    android:installLocation="auto">

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="34" />

    <!-- Разрешение для overlay (SYSTEM_ALERT_WINDOW) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        android:label="osu!"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="sh.ppy.osulazer.fileprovider"
            android:grantUriPermissions="true"
            android:exported="false">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

        <!--
            <activity> для OsuGameActivity НАМЕРЕННО убрана отсюда.
            Она генерируется автоматически из атрибута [Activity(..., MainLauncher = true)]
            в OsuGameActivity.cs при сборке Xamarin/MAUI.
            Дублирование вызывало конфликт манифеста и ломало установку APK.
        -->

    </application>

</manifest>
